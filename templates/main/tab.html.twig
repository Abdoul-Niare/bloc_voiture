{# Niare #}

{% extends 'base.html.twig' %}

{% block title %}Annonce
{% endblock %}

{% block body %}

<div class="row">
    <h1 class="title next-center nt-4 text-dark">Les annonces appartenant à {{marq.name}}</h1>
    
	<div class="row d-flex justify-content-center col-2 ">
	{% for annonce in marq.annonces %}
		<div class="card" style="width: 18rem, ">
		<img class="card-img-top" src="..." alt="Card image cap">
			<div class="card-body">
				<a href="#" class="card-title">{{annonce.marque}}</a>
				<p class="card-text">{{annonce.description|raw}}</p>
				<a href="#" class="btn btn-primary">Go</a>	
			</div>
		</div>
	{% endfor %}
	</div>
    </div>
    {%endblock%}